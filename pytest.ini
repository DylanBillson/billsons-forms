[pytest]
testpaths = tests
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
timeout = 30
cache_dir = /tmp/pytest_cache
filterwarnings =
    ignore::DeprecationWarning
markers =
    integration: test exercises service/database integration rather than an isolated unit
    postgres: test requires TEST_DATABASE_URL and PostgreSQL-specific semantics
